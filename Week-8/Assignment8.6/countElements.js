/*
Problem: Take an array input from the user and return the count of elements.
Note - The time complexity must be O(1). So you cannot use a loop.
 */
function countElements(arr) {
  return arr.length;
}

console.log(countElements([1, 2, 3, 4, 5]));
